# Minimal config for E2E core tests
[general]
persona = "test-agent"

[providers.test]
kind = "anthropic"
# key injected via environment

[tools]
enabled = ["shell", "read", "write", "edit", "web_fetch", "web_search_tool", "http_request"]

[security]
approval_mode = "auto"
